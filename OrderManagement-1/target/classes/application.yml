spring:
  application:
    name: OrderManagement
  datasource:
    url: jdbc:mysql://localhost:3306/ordermanagement2_db
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  messages:
    basename: messages


server:
  port: 8092