spring:
  application:
    name: OrderManagement
  datasource:
    url: jdbc:mysql://localhost:3306/OrderManagement_lenscart
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  messages:
    basename: messages


server:
  port: 8092
#eureka:
 # client:
  #  service-url:
    #  defaultZone: http://localhost:8761/eureka